export enum Path {
  Garage = "garage",
  Engine = "engine",
  Winners = "winners",
}

export enum Method {
  POST = "POST",
  GET = "GET",
  PATCH = "PATCH",
  PUT = "PUT",
  DELETE = "DELETE",
}

export enum SortWinners {
  Id = "id",
  Wins = "wins",
  Time = "time",
}

export enum Order {
  Asc = "ASC",
  Desc = "Desc",
}
